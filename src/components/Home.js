import React from 'react';

const Home = () => {
    return (
        <div>
            <h2 style={{textAlign: 'center'}}>HealthRecord</h2>
            <p style={{textAlign: 'center'}}> Pick a Diagnosis <br/>
            Click on each diagnosis to enter a medication and whether it has been called into the pharmacy or not.</p>
        </div>
    );
}

export default Home;